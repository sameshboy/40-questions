// Program 2: Input two numbers and print their sum
#include <stdio.h>

int main() {
    int a, b;
    // Input two numbers from user
    printf("Enter two integers: ");
    scanf("%d %d", &a, &b);
    // Calculate and print the sum
    printf("Sum = %d\n", a + b);
    return 0;
}
